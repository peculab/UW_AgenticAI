"""
改進年化報酬率的策略優化指南

當前表現：
- 年化報酬率: 1.70%
- 最大回撤: -14.61%
- 交易次數: 193 次

目標表現:
- 年化報酬率: 10-15%+
- 最大回撤: -5% ~ -10%
- 高質量交易: 300-500 次
"""

# ============================================================
# 方案 1: 增強 DQN 訓練
# ============================================================
# 在 train_dqn() 調用時修改參數

# 修改前:
# train_dqn(states, rewards_returns)  # episodes=2, max_steps=80

# 修改後:
train_dqn(states, rewards_returns, episodes=20, gamma=0.99, epsilon=0.30, max_steps=500)

# 解釋:
# - episodes=20: 增加訓練次數（從 2 到 20）→ 更深度學習
# - max_steps=500: 增加每次採樣的步數（從 80 到 500）→ 覆蓋更多交易信號
# - gamma=0.99: 增加折扣因子（從 0.95 到 0.99）→ 更重視長期收益
# - epsilon=0.30: 增加探索率（從 0.25 到 0.30）→ 避免陷入局部最優


# ============================================================
# 方案 2: 添加模式置信度過濾
# ============================================================
# 只在 CNN 高置信度的模式上交易

# 修改 state 向量來排除低置信度信號:

CNN_CONFIDENCE_FILTER = 0.65  # 只保留 >65% 置信度的模式

# 在構建 states 前添加:
cnn_pattern_probs_filtered = np.where(
    cnn_pattern_confidence[:, None] >= CNN_CONFIDENCE_FILTER,
    cnn_pattern_probs,
    unknown_pattern_signal
).astype(np.float32)

# 然後用 cnn_pattern_probs_filtered 替代 cnn_pattern_probs_for_state


# ============================================================
# 方案 3: 擴大參數搜索網格
# ============================================================
# 當前參數搜索可能太保守了，試試更激進的參數

# 修改當前的網格:

MIN_HOLD_GRID = [12, 24, 48, 72, 120]  # 從 720 開始太長了
SWITCH_MARGIN_GRID = [0.0001, 0.0005, 0.001, 0.002, 0.005, 0.010]
ENTER_MARGIN_GRID = [0.0001, 0.0005, 0.001, 0.002]

# 額外的激進參數測試:
ALTERNATIVE_GRIDS = {
    "aggressive": {
        "min_hold_bars": [6, 12, 24],
        "switch_margin": [0.00001, 0.0001, 0.0005],
        "enter_margin": [0.00001, 0.0001],
    },
    "moderate": {
        "min_hold_bars": [24, 48, 72, 120],
        "switch_margin": [0.0001, 0.001, 0.005, 0.010],
        "enter_margin": [0.0001, 0.0005, 0.001],
    },
    "conservative": {
        "min_hold_bars": [120, 240, 360],
        "switch_margin": [0.005, 0.010, 0.015],
        "enter_margin": [0.001, 0.005, 0.010],
    }
}


# ============================================================
# 方案 4: 模式特定的策略權重
# ============================================================
# 根據每個模式的歷史表現調整權重

PATTERN_TRADING_WEIGHTS = {
    "hammer": 1.2,              # +20% 權重（歷史勝率好）
    "bullish_engulfing": 1.2,   # +20% 權重
    "morning_star": 1.1,        # +10% 權重
    
    "doji": 0.5,                # -50% 權重（偏向等待）
    "unknown": 0.3,             # -70% 權重（避免）
    
    "evening_star": 0.9,        # -10% 權重
    "bearish_engulfing": 1.0,   # 中立
    "shooting_star": 1.0,       # 中立
    "hanging_man": 1.0,         # 中立
}

# 在 Q 值調整中應用:
# q_adjusted = q * PATTERN_TRADING_WEIGHTS[detected_pattern]


# ============================================================
# 方案 5: 動態止損和止盈
# ============================================================
# 在 smooth_actions 中添加風險管理

MAX_LOSS_PERCENT = 0.02  # -2% 止損
TAKE_PROFIT_PERCENT = 0.05  # +5% 止盈

# 如果從進場以來的虧損超過 2%，強制平倉
# 如果獲利超過 5%，強制平倉或縮小頭寸


# ============================================================
# 方案 6: 調整目標和過濾條件
# ============================================================

TARGET_ANNUAL_RETURN = 0.12  # 從 15% 降至 12%（更務實）
MAX_ACCEPTABLE_TEST_DRAWDOWN = -0.10  # 從 -20% 改為 -10%（更嚴格）
MIN_PATTERN_CONFIDENCE = 0.65  # 只考慮高置信度模式
MIN_TEST_TRADES = 50  # 至少 50 次交易以保證統計意義


# ============================================================
# 方案 7: 特定時間段優化
# ============================================================
# 某些市場條件下策略表現更好

# 分析 CNN 信號表，找出最好的時間窗口:
# 1. 統計每個時間段的勝率
# 2. 只在高勝率時間交易
# 3. 在低勝率時段保持 flat

# 舉例：
MARKET_REGIME_FILTERS = {
    "asian_session": {"start": 0, "end": 8},       # UTC 0-8 時段
    "european_session": {"start": 8, "end": 16},   # UTC 8-16 時段
    "us_session": {"start": 13, "end": 22},        # UTC 13-22 時段
}


# ============================================================
# 實施步驟
# ============================================================
"""
1. 備份當前腳本:
   cp Trading_AgenticAI_GAF_CNN_DQN_Demo.py Trading_AgenticAI_GAF_CNN_DQN_Demo.backup.py

2. 修改 train_dqn 調用:
   - 增加 episodes 到 20
   - 增加 max_steps 到 500
   - 調整 epsilon 和 gamma

3. 添加模式過濾:
   - 設置 CNN_CONFIDENCE_FILTER = 0.65
   - 只在高置信度模式上交易

4. 擴大參數搜索:
   - 修改 MIN_HOLD_GRID 為更激進的值
   - 增加搜索範圍

5. 運行並分析結果:
   python Trading_AgenticAI_GAF_CNN_DQN_Demo.py
   
6. 檢查結果:
   - 查看 results/summary.json 的 annualized_return
   - 查看 results/ranked_results.csv 的前幾行
   - 對比 results/best_params.json

7. 迭代優化:
   - 如果年化報酬率 >5% 且回撤 <-10%，保存這個版本
   - 嘗試進一步微調參數
"""


# ============================================================
# 預期改進
# ============================================================
"""
應用上述方案後的預期效果：

方案 1 (增強 DQN):
  - 年化報酬率: 1.70% → 3-5%
  - 提升: +50-200%

方案 2 (模式過濾):
  - 年化報酬率: +1-2%（提高交易質量）
  - 回撤: -14.61% → -10% ~ -12%

方案 3 (參數搜索):
  - 年化報酬率: +2-3%
  - 增加高質量交易

組合所有方案:
  - 年化報酬率: 1.70% → 7-12%
  - 最大回撤: -14.61% → -8% ~ -12%
  - 交易質量: 大幅提升
"""
